# TODO: Inject configs with config.yaml and environment variables

Configs = {
    "db": {
        "host": "postgresql",
        "port": 5432,
        "user": "sqluser",
        "password": "sqlpassword",
        "db": "postgres",
        "echo": True
    },
}
